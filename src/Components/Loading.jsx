import React from "react";
import './Loading.css'

function Loading() {
    return (
        <section className="Loading">
            <p>Loading...</p>
        </section>
    )
}

export default Loading;